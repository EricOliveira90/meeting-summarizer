## Evaluator feedback — round 2

VERDICT: REVISE
GAPS: 4
RE_RAISED_GAPS: 2

### If REVISE, specific gaps:
- **In scope / Non-goals / Test plan:** Readiness requires, "`codex login status` ... Exit zero passes authentication" and "A failed probe makes later probes `not_checked`," while automated tests require only "a controlled executable." The installed `codex` in this target environment exits 1 with `Login is not required. OpenAI Codex uses Bedrock via managed credentials.`, so setup would report authentication `failed` and skip the model probe even though managed Codex credentials are usable. The setup test also does not assert the exact probe argv/order or short-circuit outcomes, so a fake-only pass cannot detect this production incompatibility. Define an authentication probe/outcome that supports the installed managed-credential CLI and test the exact commands, order, and each short-circuit branch. This violates UAT-verifiability and materially repeats round-1's process-protocol gap.
- **In scope / Test plan:** The adapter limits only "stdout/stderr byte limits," and the overflow test covers only "oversized stdout, or oversized stderr," but the actual Summary comes from `--output-last-message <temp-output>`. Codex can write an arbitrarily large final message to that file without crossing either stream limit, after which the adapter may read it unbounded and still return success. Add a final-message byte limit, an exact failure result, and a controlled oversized-`<temp-output>` test. This violates falsifiability and the promised bounded execution/output behavior.
- **In scope / Test plan:** Cleanup is promised only when "Abort, timeout, or either stream exceeding its limit" occurs, and the test likewise checks removal only for those modes. Success, classified nonzero exits, ordinary process failure, and malformed output may therefore leave `<temp-output>` containing sensitive Summary content on disk. Require cleanup after every terminal outcome and assert no temp artifact after success and each failure mode. This violates boundary explicitness and UAT-verifiability.
- **In scope / Test plan:** The contract groups "`AUTHENTICATION`, `PERMISSION`, `MODEL`" at one precedence level and tests unspecified "mixed-signal fixture modes." It does not define which category wins when one nonzero stderr contains two classified stimuli, nor name concrete conflicts that exercise each boundary in the stated precedence chain. Define either mutual exclusivity or an exact order among classified stderr categories, and list mixed cases that prove abort over timeout, timeout over overflow, overflow over classified stderr, and classified stderr over generic exit/malformed output. This violates falsifiability and materially repeats round-1's classification-precedence gap.
