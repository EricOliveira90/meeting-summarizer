## Evaluator feedback — round 1

VERDICT: REVISE
GAPS: 9
RE_RAISED_GAPS: 0

### If REVISE, specific gaps:
- **In scope / Test plan:** In scope requires, "no Codex credential sentinel, Transcript, Summary, provider model, or Summary request crosses HTTP," but this slice necessarily obtains the Transcript through `GET /jobs/:id/transcript`, so the Transcript does cross HTTP from server to notebook. The test plan silently narrows this to "server requests/logs contain none," leaving the scope and verdict inconsistent. State the directional boundary explicitly: the Transcript response is allowed, while no subsequent notebook-to-server request may contain the credential, model, Transcript, Summary, or Summary request; then inspect request bodies/headers and logs accordingly. This violates falsifiability and boundary explicitness.
- **In scope / Non-goals / Test plan:** The contract says the adapter "invokes the configured Codex model locally and non-interactively," and that "process protocol is proven with a controlled executable," but it never locks the executable, exact argv, stdin framing, or source of the final Summary. A controlled executable can accept any invented protocol, so "captured argv proves non-interactive model selection" can pass even when the adapter is incompatible with installed `codex exec`; this also omits the PRD roadmap's requirement that exact executable flags be contract-tested. Specify the exact production command/input/output protocol and exact executable, authentication, and model probe commands and outcomes. This violates UAT-verifiability.
- **In scope / Test plan:** The failure contract assigns `AUTHENTICATION`, `PERMISSION`, `MODEL`, `PROCESS`, and other categories, while the plan merely supplies "auth, permission, model, spawn, and nonzero-exit modes." It does not define the child exit codes/stdout/stderr for those modes or the precedence when signals conflict, so an adapter can classify arbitrary diagnostics and still satisfy a matching fake. Add concrete process stimuli and exact expected structured results, including redacted messages and retryability. This violates falsifiability.
- **In scope / Test plan:** The contract both "accepts only a non-empty Summary result" and classifies "empty/invalid output" as `MALFORMED_OUTPUT`, while the plan separately names "empty/malformed output." No Summary format or invalid-output rule is defined, so any non-whitespace text could be accepted or rejected at implementation discretion. Define normalization and the exact malformed cases, or narrow the category to empty/whitespace-only output. This violates falsifiability.
- **In scope / Test plan:** The starting condition is "a verified local Transcript," but the only success case assumes "`READY` with a verified Transcript." In the current client, `READY` also survives a failed download and does not persist a Transcript path, so it is not evidence that a local artifact exists or was verified. Define the exact local verification predicate/path and add missing, unreadable, empty, and changed-artifact cases proving Codex is not invoked and the Job remains incomplete. This violates falsifiability and UAT-verifiability.
- **In scope / Test plan:** In scope says, "Provider, validation, write, verification, rename, state-write, cancellation, and timeout failures leave the Job incomplete," but only Provider failures have defined operation/category/message/retryability/timestamp outcomes. The test then expects "the expected structured failure" for "each provider or Summary commit fault" without defining that expectation for Transcript validation, temporary write/read/mismatch, rename, cleanup, or completion-state persistence failure. Define the durable status and exact failure record for each fault, including what remains observable when the state write itself fails. This violates falsifiability.
- **In scope / Test plan:** The contract promises, "Retryable or explicitly retried terminal Summary failures reuse the verified Transcript and rerun only Codex," but it defines neither the explicit retry action nor the durable transition that makes a Summary failure eligible for Manual Sync. The current `JobManager.retryJob` resets `FAILED`/`ABANDONED` to `WAITING_UPLOAD`, which would route through server lookup/upload logic, directly conflicting with the planned zero server/upload/download calls. Specify the operation-aware states before and after automatic and explicit retry, invoke the public retry action in the test, and assert those persisted transitions. This violates boundary explicitness and UAT-verifiability.
- **In scope / New patterns / Test plan:** Success promises "ordered local stage timestamps," but no local stage names, timestamp fields, required endpoints, or order are defined; "optional persisted ... stage metadata" makes the obligation weaker still. The assertion "stage timestamps are ordered" therefore has no fixed expected value and could pass with one arbitrary timestamp. Name the required stages/fields and exact ordering, and use an injected clock to prove every boundary. This violates falsifiability.
- **Scope lock / Files expected to change / Test plan:** The slice combines three sequential deliverables: setup plus three readiness probes; a cancellable child-process supervisor with timeout, bounded dual-stream capture, protocol parsing, classification, and redaction; and a durable workflow/schema/retry/Jobs Hub migration plus a listening-server acceptance journey. The expected change list spans 20 production/test files and nine independent test-plan bullets. That is not a single-session TDD slice. Split the Codex adapter/setup contract from the durable Summary commit/retry/Jobs Hub integration, or materially reduce the failure matrix and UI/schema surface. This violates single-session feasibility.
